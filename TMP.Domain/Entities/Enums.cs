namespace TMP.Domain.Entities
{
    public enum Gender
    {
        MALE, FEMALE, NA
    }

    public enum SubType
    {
        BASE, PREMIUM
    }

    public enum MetricType
    {
        ERROR, DISTANCE, TIME, REP, WEIGHT
    }
}
